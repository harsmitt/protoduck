name: Build DuckDB 1.5.5

on:
  push:
    branches:
      - build-duckdb-1.5.5
  workflow_dispatch:

jobs:
  build:
    name: Build and test on Ubuntu
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Build Protoduck
        run: cargo build --release

      - name: Set DuckDB target version for test harness
        run: sed -i 's/TARGET_DUCKDB_VERSION=v1.5.4/TARGET_DUCKDB_VERSION=v1.5.5/' Makefile

      - name: Run SQL tests
        run: make test_release
